define(
  [],
  function() {
    /**
      HUD - the main entry point
    */
    return class HUD {
      /**
       * constructor()
       * @description constructs the heads up display
       * @param player the player playing the game
       */
       constructor(player) {
         this._player = player;
       }


       /**
        * initSVG()
        * @description initializes the svgs of the hud
        */
        initSVG() {

        }
    }
  }
)
